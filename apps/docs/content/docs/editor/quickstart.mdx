---
title: Open and edit your first DOCX
navTitle: Quickstart
description: Install the browser Editor, open a sample DOCX, and export your first edit.
---

Follow this guide from top to bottom. You will open a sample agreement, change its text, and export a new DOCX.

## 1. Create a project

Create a Vite project and install SuperDoc:

<FrameworkExampleTabs>
  <FrameworkExample filename='Terminal' framework='Vanilla'>
    <include>../../../snippets/editor/quickstart-vanilla.sh</include>
  </FrameworkExample>
  <FrameworkExample filename='Terminal' framework='React'>
    <include>../../../snippets/editor/quickstart-react.sh</include>
  </FrameworkExample>
</FrameworkExampleTabs>

The `latest` tag installs SuperDoc v2. Pin the resolved version before shipping so upgrades stay deliberate.

## 2. Add the sample document

Download the sample and save it as `public/sample.docx`:

<FileDownload
  href='/fixtures/sample-nda.docx'
  label='Download the sample document'
  description='Synthetic agreement with headings, paragraphs, and a list'
  fileType='DOCX'
/>

Vite serves `public/sample.docx` at `/sample.docx`.

## 3. Add the page

Add the application surface. Vanilla mounts SuperDoc into `#editor`; the React wrapper creates that container for you.

<FrameworkExampleTabs>
  <FrameworkExample filename='index.html' framework='Vanilla'>
    <include>../../../../../examples/vanilla/index.html</include>
  </FrameworkExample>
  <FrameworkExample filename='src/main.tsx' framework='React'>
    <include>../../../../../examples/react/src/main.tsx</include>
  </FrameworkExample>
</FrameworkExampleTabs>

## 4. Open the document

Add the Editor and enable export after the document opens:

<FrameworkExampleTabs>
  <FrameworkExample filename='src/main.ts' framework='Vanilla'>
    <include>../../../../../examples/vanilla/src/main.ts</include>
  </FrameworkExample>
  <FrameworkExample filename='src/App.tsx' framework='React'>
    <include>../../../../../examples/react/src/App.tsx</include>
  </FrameworkExample>
</FrameworkExampleTabs>

Run the project:

```bash
pnpm dev
```

Open the printed URL. The agreement should appear, and **Export DOCX** should become enabled. That button stays disabled
until `onReady` confirms that the document is open.

## 5. Make an edit and export

Replace a word in the agreement, then select **Export DOCX**. The browser downloads `sample-edited.docx`.

<Callout variant='success' title='Check the exported document'>
  Reopen `sample-edited.docx` in Word or SuperDoc. Confirm that your edit is present and that the heading, paragraphs,
  and list still have their original formatting.
</Callout>

## What just happened

- `selector` mounted the Vanilla Editor; the React wrapper owned its container.
- `document` gave the browser a DOCX URL to open.
- `onReady` marked the first safe moment to enable document actions.
- `export()` created the edited DOCX and triggered the download.

If the document does not appear, check the `onContentError` or `onException` message in the browser console. They
report import and startup failures instead of leaving an unexplained empty mount point.

The complete projects are available for [Vanilla](https://go.superdoc.dev/examples/vanilla) and
[React](https://go.superdoc.dev/examples/react).

## Choose what to build next

- [Load and save documents](/editor/load-and-save-documents) to open user files or save exports to your backend.
- [Configure the Editor](/editor/configuration) to change startup behavior, layout, UI, and events.
- [Document modes](/editor/document-modes) to compare direct edits, tracked suggestions, and read-only viewing.
